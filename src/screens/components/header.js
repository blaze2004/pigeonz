import React from "react";
import { Text } from '@aws-amplify/ui-react';

function Header() {
    return (
        <nav>
            <Text>
                Welcome, 
            </Text>
        </nav>
    );
}

export default Header;